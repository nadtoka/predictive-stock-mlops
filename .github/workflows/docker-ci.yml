name: MLOps Build, Test and Push to GHCR

on:
  push:
    branches: [ "main" ]
    paths-ignore:
      - 'README.md'
      - '.gitignore'
      - 'data/**'

  workflow_dispatch:
    inputs:
      ticker:
        description: 'Які тікери протестувати вручну? (через кому)'
        required: false
        default: ''

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-test-push:
    runs-on: self-hosted
    permissions:
      contents: read
      packages: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Extract Docker metadata
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
        tags: |
          type=raw,value=latest
          type=sha,format=short

    - name: Build Docker image locally
      run: |
        docker build -t test-image:local .

    - name: Run Smoke Test (Multi-Ticker Verification)
      run: |
        mkdir -p ./test_data
        
        TARGET_TICKERS="${{ github.event.inputs.ticker }}"
        if [ -z "$TARGET_TICKERS" ]; then
          TARGET_TICKERS="${{ vars.STOCK_TICKER }}"
        fi
        if [ -z "$TARGET_TICKERS" ]; then
          TARGET_TICKERS="AAPL"
        fi
        
        echo "📡 Запускаємо перевірку для тікерів: $TARGET_TICKERS"
        
        docker run --rm \
          -e STOCK_TICKER="$TARGET_TICKERS" \
          -e HF_TOKEN="${{ secrets.HF_TOKEN }}" \
          -e HF_REPO="" \ 
          -v $(pwd)/test_data:/app/data \
          test-image:local

    - name: Log in to GitHub Container Registry
      run: |
        echo "${{ secrets.GITHUB_TOKEN }}" | docker login ${{ env.REGISTRY }} -u ${{ github.actor }} --password-stdin
    
    - name: Push Docker image to GHCR
      run: |
        # Читаємо теги рядок за рядком через пайп
        echo "${{ steps.meta.outputs.tags }}" | while read -r tag; do
          # Перевіряємо, чи рядок не порожній
          if [ ! -z "$tag" ]; then
            echo "🏷️ Тегуємо та пушимо: $tag"
            docker tag test-image:local "$tag"
            docker push "$tag"
          fi
        done
